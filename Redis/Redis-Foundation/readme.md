### Redis foundation 

### Practice with Docker
1. First create docker compose file and setup the docker compose file

***Docker initial command***
- docker compose up == initally pull docker images from docker hub
