# 🎭 Abstraction (অ্যাবস্ট্রাকশন) — Python OOP

**Abstraction** মানে হলো **ক্লাস বা অবজেক্টের শুধু প্রয়োজনীয় বৈশিষ্ট্য দেখানো এবং অপ্রয়োজনীয় ডিটেইল লুকানো।**  
এটি OOP-এর একটি মূল স্তম্ভ:  
**Encapsulation, Abstraction, Inheritance, Polymorphism**

> সহজভাবে: তুমি কার্ড চালাতে জানো, কিন্তু ভিতরের সার্কিট বোঝার দরকার নেই।  
> এটিই অ্যাবস্ট্রাকশন।
> মোট কথা তোমাকে একটা ইন্টারফেস দেওয়া হবে তুমি জাস্ট ইন্টারফেসটা ব্যবহার করে কাজ করবে, ইন্টারফেসের ভীতরে কি হচ্ছে সেটা জানার দরকার নেই।

---

## Python-এ Abstraction

Python-এ **abstract class** এবং **abstract method** ব্যবহার করে অ্যাবস্ট্রাকশন করা হয়।  
Abstract class-কে সরাসরি instantiate করা যায় না।  
Child class অবশ্যই abstract method implement করবে।  

---

## উদাহরণ (Python)

```python
from abc import ABC, abstractmethod

# Abstract Class
class Vehicle(ABC):
    @abstractmethod
    def start(self):
        pass  # কীভাবে শুরু হবে, Child class-এ define হবে

    @abstractmethod
    def stop(self):
        pass  # কীভাবে বন্ধ হবে, Child class-এ define হবে

# Child Class
class Car(Vehicle):
    def start(self):
        print("Car চালু হলো")

    def stop(self):
        print("Car বন্ধ হলো")

class Bike(Vehicle):
    def start(self):
        print("Bike চালু হলো")

    def stop(self):
        print("Bike বন্ধ হলো")

# ব্যবহার
vehicles = [Car(), Bike()]

for v in vehicles:
    v.start()
    v.stop()
```
## Output
```python
Car চালু হলো
Car বন্ধ হলো
Bike চালু হলো
Bike বন্ধ হলো
```
---
Abstract class Vehicle শুধু ইন্টারফেস দিলো।
Child class-এ কীভাবে start এবং stop কাজ করবে define করা হলো।
বাইরের কোড শুধু Vehicle interface ব্যবহার করছে।