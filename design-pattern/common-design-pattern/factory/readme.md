## Factory Design Pattern (বাংলায়)
ফ্যাক্টরি ডিজাইন প্যাটার্ন হলো একটি **Creational Design Pattern** যা অবজেক্ট তৈরির লজিককে ক্লায়েন্ট কোড থেকে আলাদা করে ফেলে। 

---
## 📌 Factory Pattern কী?
Factory Design Pattern এমন একটি ডিজাইন প্যাটার্ন যা:
- 👉 অবজেক্ট তৈরি করার লজিক আলাদা করে ফেলে।
- 👉 ব্যবহারকারীকে না জানিয়ে কোন ক্লাসের অবজেক্ট তৈরি হবে সেটা নির্ধারণ করে।

> **সহজ ভাষায়:** “কোন অবজেক্ট বানাতে হবে সেটা বলুন, কীভাবে বানানো হবে সেটা Factory দেখবে।”

---

## Factory কেন দরকার?
যখন আপনার প্রোজেক্টে নিচের পরিস্থিতিগুলো তৈরি হয়:
1. একাধিক ক্লাসের অবজেক্ট তৈরি করতে হয়।
2. কোডে বারবার `if-else` বা `switch` ব্যবহার করে অবজেক্ট নির্ধারণ করতে হচ্ছে।
3. কোডকে **Scalable** এবং **Maintainable** করতে চান।

---

## Factory Pattern-এর মূল ধারণা
১. **Common Interface:** একটি বেস ক্লাস বা ইন্টারফেস যা সব কংক্রিট ক্লাস ফলো করবে।  
২. **Concrete Classes:** বিভিন্ন স্পেসিফিক ক্লাস (যেমন: Bkash, Nagad)।  
৩. **Factory Class:** একটি নির্দিষ্ট ক্লাস যা লজিক অনুযায়ী অবজেক্ট তৈরি করে দেয়।

---

## বাস্তব উদাহরণ (Real-world Scenarios)
- **Payment Gateway:** Bkash, Nagad, Stripe.
- **Notification System:** Email, SMS, Push.
- **File Parser:** PDF, CSV, JSON.
- **Django:** Serializer বা service layer-এ অবজেক্ট হ্যান্ডলিং।

---

## 💻 কোড ইমপ্লিমেন্টেশন (Python Example)

```python
from abc import ABC, abstractmethod

# 1. Common Interface
class Notification(ABC):
    @abstractmethod
    def send(self, message):
        pass

# 2. Concrete Classes
class EmailNotification(Notification):
    def send(self, message):
        return f"Sending Email: {message}"

class SMSNotification(Notification):
    def send(self, message):
        return f"Sending SMS: {message}"

# 3. Factory Class
class NotificationFactory:
    @staticmethod
    def get_notification(type):
        if type == "email":
            return EmailNotification()
        elif type == "sms":
            return SMSNotification()
        raise ValueError("Unknown Type")

# Client Code
notifier = NotificationFactory.get_notification("email")
print(notifier.send("Hello Factory!"))